const playerState = {
    weapon: 'Fists',
    hp: 100,
    dmg: 10,
    attSpd: 1,
    def: 10,
    eva: 10
}

const playerReducer = (state = playerState) => {
    return state;
}

export default playerReducer;