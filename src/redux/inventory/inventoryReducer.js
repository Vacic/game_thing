const inventoryState = {
    currentItems: ['Wooden Shield', 'Wooden Boots', 'Wooden Armor', 'Wooden Sword', 'Wooden Pants']
}

const inventoryReducer = (state = inventoryState) => {
    return state;
}

export default inventoryReducer;